export default function LoginPage() {
  return (
    <main className="page">
      <div className="card" style={{ maxWidth: 520, margin: "0 auto" }}>
        <h1>Login</h1>
        <p>Use the backend endpoint <code>/api/v1/auth/login</code> from this screen in the next iteration.</p>
      </div>
    </main>
  );
}
